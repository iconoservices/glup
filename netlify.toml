[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Fallback SPA para rutas no pre-generadas (las reales se sirven como HTML)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
